replicaCount: 1

elasticsearchHosts: "http://elasticsearch-master.logging.svc.cluster.local"

service:
  type: LoadBalancer
  port: 5601

resources:
  requests:
    cpu: 100m
    memory: 512Mi
  limits:
    cpu: 500m
    memory: 1Gi
